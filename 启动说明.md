# A2Tools DPS Meter - 启动说明

## 前置要求

在运行前，请确保已安装：
- **Node.js** (https://nodejs.org) - 用于运行开发服务器
- **Rust** (https://rustup.rs) - Tauri 需要 Rust 工具链

## 快速开始

### 直接使用 npm 命令

```bash
# 开发模式
npm run tauri dev

# 构建发布版本
npm run tauri build

# 仅启动前端开发服务器
npm run dev
```

## 构建发布版本


## 项目结构

```
A2Tools-DPS-Meter-1/
├── src/               # 前端源代码
├── src-tauri/         # Tauri 后端代码
├── public/            # 静态资源
├── start-dev.bat      # 完整启动脚本（带检查）
├── quick-dev.bat      # 快速启动脚本
├── build.bat          # 构建脚本
└── package.json       # 项目配置
```

## 常见问题

### 1. 首次运行慢？
首次运行会下载和编译 Rust 依赖，这是正常的，后续会快很多。

### 2. 端口被占用？
Tauri 默认使用 1420 端口，确保该端口未被其他程序占用。

### 3. Node.js 版本要求？
建议使用 Node.js 18 或更高版本。

## 开发建议

- 修改 `src/` 或 `public/` 下的文件会自动热重载
- 修改 `src-tauri/` 下的 Rust 代码需要重新编译（会自动重启）
